// University of Venda customer page redirect
// This page will redirect to the custom customer page with the ID parameter

import { redirect } from 'next/navigation';

export default function UnivenCustomersPage() {
  // This page acts as a redirect to maintain the URL structure
  // The actual customer page is at /user/customers/univen/[id]/page.tsx
  return null;
}