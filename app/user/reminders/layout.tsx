export const metadata = {
  title: 'Reminders | DCMS',
  description: 'View and manage your daily, weekly, and monthly reminders'
};

export default function RemindersLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
