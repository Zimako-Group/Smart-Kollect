export default function SettlementsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

export const metadata = {
  title: "Settlements | DCMS",
  description: "Manage and track settlement transactions"
};
