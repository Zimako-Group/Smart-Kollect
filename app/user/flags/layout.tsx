export const metadata = {
  title: "Flags | DCMS",
  description: "View and manage flagged accounts"
};

export default function FlagsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
