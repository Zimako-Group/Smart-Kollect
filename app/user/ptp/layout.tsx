export const metadata = {
  title: "PTP (Cash) | Zimako DCMS",
  description: "View and manage Promise To Pay (Cash) transactions"
};

export default function PTPLayout({ children }: { children: React.ReactNode }) {
  return children;
}
