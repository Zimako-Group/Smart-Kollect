export const metadata = {
  title: "Contact Rate | DCMS",
  description: "View your call contact rates and performance metrics"
};

export default function ContactRateLayout({ children }: { children: React.ReactNode }) {
  return children;
}
